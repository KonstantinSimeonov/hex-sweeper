{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha -R spec ./test/*",
    "start": "node ./src/server.js",
    "dev-start": "nodemon ./src/server.js",
    "package-dist": "cd ./src && npm install && gulp clean && gulp dist",
    "postinstall": "cd ./src && npm install"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "gulp": "^3.9.1",
    "gulp-clean": "^0.3.2",
    "gulp-cli": "^1.2.2",
    "gulp-copy": "^1.0.0",
    "gulp-rename": "^1.2.2",
    "mocha": "^3.2.0"
  }
}
